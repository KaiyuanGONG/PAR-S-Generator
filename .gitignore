# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/

# PyInstaller
*.spec.bak
dist/
build/

# Data outputs (large files - do not commit)
output/
data/
*.npz
*.i33
*.h33
*.bat
!simind/ge870_czt.smc

# SIMIND binary (not redistributable - user must provide)
simind/simind.exe
simind/*.dll
simind/*.cr3

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Logs
*.log
logs/

# Settings (user-specific)
*.ini

# Pytest / local temp artifacts
.pytest_cache/
.pytest_tmp/
.test_tmp/
.test_tmp_manual/
foo_tmp/
bar_tmp/
baz_tmp/
tests/_tmp_pytest/
.par-s-generator/
.tmp_settings.json

# Notebook binary data (large generated files)
notebook/*.bin

# Scientific runtime payloads (large; keep local, commit ledgers/QC instead)
experiments/**/*.bin
experiments/**/*.a00
experiments/**/*.ict
experiments/**/*.dmi
experiments/**/*.smi
experiments/**/*.b[0-9][0-9]
experiments/**/*.s[0-9][0-9]
runs/**/*.bin
runs/**/*.a00
runs/**/*.npz

# The only authorized local Gate A pilot output. Reports and manifests stay
# reviewable in place but are not mixed into the implementation commit.
runs/gate-a-v2-master-100-20260819/

# Keep shared pytest config tracked
!pytest.ini
